name: dhr-7
services:
  - name: web
    build_command: |
      npm install
      npm run build:server
    dockerfile_path: Dockerfile
    source_dir: .
    github:
      branch: main
      deploy_on_push: true
      repo: alankirby/dhr-7
